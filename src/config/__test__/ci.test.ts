describe('CI - Sample Test', () => {
  it('Need quantum computer to solve this problem', () => {
    expect(true).toBe(true);
  });

  it('it\'s calculas', () => {
    const a = 1;
    const b = 2;
    expect(a + b).toBe(3);
  });
});
